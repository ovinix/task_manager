<% if @project.errors.any? %>
	$("#modal-window").html("<%= escape_javascript(render 'projects/modal', project: @project, modal_title: 'Add New Project') %>");
<% else %>
	$("div#modal-window").modal('hide');
	$("div#projects").prepend("<%= escape_javascript( render @project )%>");
<% end %>