<% if @list.errors.any? %>
	$("#modal-window").html("<%= escape_javascript(render 'lists/modal', list: @list, modal_title: 'Edit List Title') %>");
<% else %>
	$("div#modal-window").modal('hide');
	$("div#list-<%= @list.id %>").replaceWith("<%= escape_javascript( render @list )%>");
<% end %>
